<?php

/*use Illuminate\Support\Facades\Http;

$lat = $_POST['lat'];
$lng = $_POST['lng'];
$nome = $_POST['nome'];
$vagas = $_POST['vagas'];
$endereco = $_POST['endereco'];

$response = Http::post(route('estacionamento.inserir'), [
    'lat' => $lat,
    'lng' => $lng,
    'nome' => $nome,
    'vagas' => $vagas,
    'endereco' => $endereco,
]);

if ($response->ok()) {
    // Sucesso, redirecionar ou exibir mensagem de sucesso
    header('Location: ../html/index.php');
} else {
    // Lidar com falha na solicitação, redirecionar ou exibir mensagem de erro
}*/
